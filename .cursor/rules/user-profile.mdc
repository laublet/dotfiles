---
description: User profile and interaction preferences for Loïc
alwaysApply: true
---

# User profile

All preferences, interaction style, and CLI tools are defined in the shared agent config.

Read **`~/dev/perso/vaults/Research/AGENTS-core.md`** for preferences and skills (`AGENTS.md` is the index). This dotfiles repo only adds `.cursor/rules/` constraints (no-secrets, neovim).
