# Pop Shell keybindings — archived

Legacy Pop!_OS 22.04 + GNOME Pop Shell. Superseded by **COSMIC** (`conf/cosmic/` + `scripts/gen-cosmic-shortcuts.sh`).

Apply was: `dconf load / < keybindings.dconf` (see `install-linux.conf.yaml` history).
